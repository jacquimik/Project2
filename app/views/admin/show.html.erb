<div class="show_products">
	<h2><%= @product.name %></h2>
		<p>
			<%= @product.price %>
			<%= @product.size %>
			<%= @product.brand %>
		</p>
		<div class="img">
			<img src="<%= @product.image %>">
		</div>

		<p>
			<%= @product.description %>
		</p>
</div>