<div class="show_products">
		<p><%= '$'@product.price %></p>
			<p><%= @product.size %></p>
			<p><%= @product.brand %></p>
		<div class="img">
			<img src="<%= @product.image %>">
		</div>

		<p>
			<%= @product.description %>
		</p>
</div>